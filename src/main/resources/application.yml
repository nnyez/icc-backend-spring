spring:
    application:
        name: fundamentos01
    datasource:
      url: jdbc:postgresql://localhost:5432/devdb-spring
      username: root
      password: root
    jpa:
      hibernate:
        ddl-auto: create-drop
      properties:
        hibernate:
          format_sql: true
          dialect: org.hibernate.dialect.PostgreSQLDialect

server:
  port: 8080